[/Script/WorldPartitionEditor.WorldPartitionEditorSettings]
; Note: HLOD builder failures don't prevent code compilation
; The code build is working correctly - this only affects World Partition HLOD generation

