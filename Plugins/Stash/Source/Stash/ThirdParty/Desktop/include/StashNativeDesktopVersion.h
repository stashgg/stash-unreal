// Single source of the desktop host version. Returned by StashNativeDesktop_GetVersion() on
// both OSes and by the macOS facade's +sdkVersion. release.yml checks it against CHANGELOG.md.
#ifndef STASH_NATIVE_DESKTOP_VERSION_H
#define STASH_NATIVE_DESKTOP_VERSION_H

#define STASH_NATIVE_DESKTOP_VERSION "2.4.0"

#endif
